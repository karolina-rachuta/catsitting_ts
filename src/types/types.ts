export type QueryFormValues = {
    catsName: string;
    numberOfCats: string;
    catsittingOption: string;
    startDate: string;
    endDate: string;
    message: string;

    name: string;
    email: string;
    phone: string;
    address: string;

    contactConsent: boolean;
};
